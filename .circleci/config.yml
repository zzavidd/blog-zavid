version: 2.1

jobs:
  deploy:
    docker:
      - image: kroniak/ssh-client
    steps:
      - run: ssh -o StrictHostKeyChecking=no -v $USER@$IP "cd blog-zavid/deploy; ls"

workflows:
  version: 2
  build-project:
    jobs:
      - deploy:
          filters:
            branches:
              only:
                - main